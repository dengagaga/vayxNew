const swiperIndexMobilOne = new Swiper(".swiperIndexMobilOne", {
    effect: "cards",
    grabCursor: true,
    initialSlide: 1,
      autoplay: {
        delay: 5000,       // Задержка 5 секунд (5000 мс)
        
    },
});